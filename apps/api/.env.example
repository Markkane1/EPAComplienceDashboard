# EPA Compliance Dashboard - Environment Configuration
# IMPORTANT: Never commit real secrets to version control
# For production, use a secrets manager (AWS Secrets Manager, Vault, etc.)

# Node environment
NODE_ENV=development

# Server Configuration
PORT=4000

# Database
MONGO_URI=mongodb://127.0.0.1:27017/punjab_compliance

# CRITICAL: JWT Secret
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be at least 32 characters long
JWT_SECRET=your-secure-random-256-bit-hex-string-change-in-production

# CRITICAL: Admin Password (will be hashed)
# MUST be at least 12 characters with uppercase, lowercase, numbers, and special characters
# Example format: MySecure!Pass123
# Change immediately after first login
ADMIN_PASSWORD=TempAdminPass123!Change-Me-Immediately

# Admin user details
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_NAME=System Administrator
ADMIN_CNIC=your-admin-cnic

# CORS - Only allow trusted domains in production
# Development: http://localhost:8080,http://127.0.0.1:8080
# Production: https://yourdomain.com,https://app.yourdomain.com
CORS_ORIGIN=http://localhost:8080,http://127.0.0.1:8080

# Public URLs
PUBLIC_BASE_URL=http://localhost:4000
APP_BASE_URL=http://localhost:8080

# File Upload Configuration
UPLOAD_DIR=./uploads
SECURE_UPLOADS_DIR=../secure-uploads

# Email/SMTP Configuration
# Leave empty to disable email
SMTP_HOST=smtp.yourmailprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@yourmailprovider.com
SMTP_PASS=your-email-password
SMTP_FROM=noreply@yourdomain.com

