# EPA Compliance Dashboard - Web Production Configuration
# Copy to apps/web/.env.production before building.

# Use your public host (domain or VM IP). Requests to /api and /uploads are prefixed with this value.
VITE_API_URL=http://YOUR_VM_PUBLIC_IP
